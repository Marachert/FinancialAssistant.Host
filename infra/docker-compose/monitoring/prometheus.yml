global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - prometheus:9090

  # Backend services should expose /metrics later through .NET/OpenTelemetry/Prometheus exporter.
  # Add service scrape targets when backend containers are introduced.
